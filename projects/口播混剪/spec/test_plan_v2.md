# 口播混剪 UI 全功能测试方案 v2.0

> **cdtest v3.4.4** | 2026-06-02 | Web 执行模式
> 测试账号：240017 | 测试环境：test1 (sc-test.changdu.ltd)

---

## 1. 测试背景

### 1.1 被测系统
雷霆素材系统 - 口播混剪功能，用于创建口播混剪自动化任务，配置 Core 类型、口播参数、混剪参数、媒资选择，生成推广投放视频素材。

### 1.2 页面范围
| 页面 | URL |
|------|-----|
| 口播混剪任务创建 | `https://sc-test.changdu.ltd/automation-video/oral-montage` |
| 自动化任务列表 | `https://sc-test.changdu.ltd/automation-video/montage-task?creatorUid=240017` |
| 审核列表 | `https://sc-test.changdu.ltd/automation-video/montage-audit` |

### 1.3 业务线
短剧类 → 雷霆素材系统（后台）

---

## 2. 测试目标与范围

### 2.1 测试目标
- 验证口播混剪任务创建流程完整性
- 验证 Core 联动逻辑（集数范围启用/禁用）
- 验证媒体批量操作（全选/反选/取消）
- 验证文案生成、BGM、音频、混剪参数等功能
- 验证免审核上传、确认并上传逻辑
- 验证跨页面数据一致性（创建→任务列表→审核列表）
- 验证边界值、异常流程的响应

### 2.2 测试范围
| 模块 | 功能点 | 用例数 |
|------|--------|--------|
| 口播混剪任务创建 | 剧集选择/Core/口播参数/BGM/媒体/按钮 | 19 |
| 自动化任务列表 | 列表展示/分页/操作 | 4 |
| 审核列表 | 列表展示/免审核逻辑 | 2 |
| 跨页面链路 | 创建→列表→审核 | 2 |

---

## 3. 测试策略

### 3.1 测试类型矩阵
| 功能点 | 正常流 | 边界值 | 异常流 | 组合场景 |
|--------|--------|--------|--------|----------|
| Core选择 | ✅ | ✅(全部4种) | ✅(取消清空) | ✅(联动集数范围) |
| 媒体选择 | ✅ | ✅(0/1/6项) | — | ✅(全选/反选/取消) |
| 口播参数 | ✅ | ✅(范围校验) | ✅(倒置) | — |
| 混剪参数 | ✅ | ✅(范围校验) | ✅(倒置) | — |
| 生成数量 | ✅ | ✅(0/负/大/非数) | ✅(空提交) | — |
| 文案生成 | ✅ | — | — | — |
| 免审核上传 | ✅ | — | — | ✅(联动审核列表) |

### 3.2 执行策略
1. **P0 用例**：5条，覆盖4种Core+媒体组合+任务列表验证 → 冒烟测试核心链路
2. **P1 用例**：8条，覆盖按钮联动/审批/Audit → 主业务流程
3. **P2 用例**：9条，覆盖边界值/分页/删除 → 补充验证
4. **P3 用例**：5条，覆盖默认值/细节 → 完整性验证

---

## 4. 历史已有逻辑 vs 本期新增逻辑

### 4.1 已有逻辑（v1.0 已验证）
- 4 种 Core 切换 + 表单配置 → TC-OM-001~004 已通过
- 媒体全选/反选/取消 → TC-OM-006 已通过
- 男女声/BGM 切换 → TC-OM-008/009 已通过
- 必填项校验 → TC-OM-010 已通过

### 4.2 本期增强验证（v2.0 新增重点）
- **Core-集数范围联动深度验证**：选Core后启用→取消Core后清空，不仅是disable/enable
- **确认生成/确认并上传按钮联动**：初始态→填充后→生成后的状态流转
- **跨页面数据一致性**：创建→自动化任务列表→审核列表的完整链路
- **边界值补充**：生成数量的负数/0/小数/非数字输入
- **集数/时长交叉校验**：开始>结束→端校验
- **免审核逻辑**：开关→任务不入审核列表

---

## 5. 风险分析

| # | 风险项 | 等级 | 来源 | 缓解措施 |
|---|--------|------|------|----------|
| 1 | 剧集代号选择器定位失败 | 🔴 高 | v1.0 问题 | 改用 eval Vue 实例方式填写 |
| 2 | Core 联动依赖剧集选择 | 🟡 中 | 需求 §6.2 | TC-OM-007 先选剧集再测Core联动 |
| 3 | DB 表结构差异(sharpengine vs 预期) | 🟡 中 | v1.0 问题 | 使用实际字段名 TgtCode/TgtName |
| 4 | 内网IP导航被安全策略拦截 | 🔴 高 | 当前环境 | 需配置OpenClaw导航白名单 |
| 5 | 生成数量无上限保护 | 🟡 中 | 需求 §6.5 | 验证999+负数处理 |
| 6 | 免审核上传影响审核列表逻辑 | 🟡 中 | 需求 §3.14 | TC-OM-023 专项验证 |

---

## 6. 环境配置

```yaml
环境:
  URL: https://sc-test.changdu.ltd
  登录方式: 工号登录 → Keycloak SSO
  测试账号: 109013 (需确认是否为240017)
  浏览器: Chrome (OpenClaw Browser)
  分辨率: 1920x1080
  DB: sharpengine_ads_asset_prod (AutoCreationTask表)
```

---

## 7. 交付物清单

| # | 交付物 | 路径 | 状态 |
|---|--------|------|------|
| 1 | 测试方案 | `spec/test_plan.md` | ✅ 已生成 |
| 2 | 测试用例(JSON) | `spec/test_cases_v2.json` | ✅ 已生成 |
| 3 | 测试用例(MD) | `spec/test_cases_v2.md` | ✅ 已生成 |
| 4 | 测试报告 | `spec/测试报告_v2.md` | 🔄 待执行后生成 |
| 5 | 截图 | `screenshots/` | 🔄 待执行后更新 |

---

## 8. 待确认项

| # | 确认项 | 说明 |
|---|--------|------|
| 1 | 测试账号 | v1.0 用 240017 (艾强)，auto-login.md 写 109013，需确认当前使用哪个 |
| 2 | 生成数量上限 | 需求 §6.5 示例999，需确认业务允许范围 |
| 3 | 删除所选作用对象 | 是用在媒体选择区还是任务列表，需与产品确认 |
| 4 | 重新生成作用范围 | 是重新生成文案还是重新生成整个任务，需确认 |
| 5 | 导航白名单 | URL 192.168.100.233 被拦截，需配置 OpenClaw 内网导航策略 |

---

> 生成时间：2026-06-02 10:13 | cdtest v3.4.4
