# 📊 数据库知识库 — 完整架构文档

> **生成时间**: 2026-04-28
> **扫描方式**: 直连 TiDB 全量扫描
> **数据库**: sharpengine_ads_asset_prod / ai_center / sharpengine_ads_global
> **用途**: 数据库知识库存储介绍，供 AI 和开发人员快速查阅

---

## 🗄️ 一、数据库总览

| 数据库 | 表数量 | 总行数（估算） | 用途 | 连接地址 |
|--------|--------|---------------|------|----------|
| `sharpengine_ads_asset_prod` | **133** | ~180 万 | 雷霆素材系统业务库（核心） | `tidb-in.changdu.ltd:4000` |
| `ai_center` | **30** | ~4400 万 | AI 大模型任务调度中心 | `tidb-in.changdu.ltd:4000` |
| `sharpengine_ads_global` | **617** | ~2.5 亿 | 全球广告/剧集/账户/BI | `tidb-in.changdu.ltd:4000` |

---

## 🏭 二、sharpengine_ads_asset_prod（133 表）— 核心业务库

### 2.1 模块全景

| 模块 | 表数 | 总行数 | 核心职责 |
|------|------|--------|----------|
| **素材管理** | 26 | ~47 万 | 素材信息、上传日志、烧录、同步、裂变、转码 |
| **任务管理** | 4 | ~24 万 | 通用任务、子任务、审核日志、大模型交互 |
| **海阅自动化** | 9 | ~6.9 万 | 海阅自动制作、模板、KOC 水印 |
| **AI 混剪** | 12 | ~1.3 万 | 海剧 AI 混剪、OCR、字幕抹除 |
| **烧录** | 1 | ~1.4 万 | 烧录资源 |
| **广告** | 4 | ~60 万 | ADX 广告投放 |
| **国剧** | 7 | ~1,100 | 国剧烧录、ADX 上传 |
| **图生图** | 5 | ~2,900 | 图片生成 |
| **其他** | 64 | ~30 万 | 文件系统、导入、动态背景等 |

### 2.2 核心表 TOP 20（按行数）

| 表名 | 行数 | 列数 | 用途 |
|------|------|------|------|
| `AdxCollectionBatchRank` | 600,000 | 20 | ADX 投放排行 |
| `AssetId` | 271,794 | 2 | 资产 ID 映射 |
| `MaterialInfo` | 237,570 | 61 | **核心素材表** |
| `MaterialUploadLog` | 226,029 | 69 | 素材上传日志 |
| `SubTaskInfo` | 181,593 | 26 | 子任务拆分（1/2/3 级） |
| `AutoCreationSubTaskLog` | 56,601 | 7 | 海阅子任务日志 |
| `AuditLog` | 42,669 | 11 | 审核日志 |
| `TaskInfo` | 16,014 | 18 | 通用任务主表 |
| `BurnResource` | 13,808 | 22 | 烧录资源 |
| `auto_material_task_log` | 13,504 | 6 | 自动素材任务日志 |
| `FileSysLog` | 6,279 | 8 | 文件系统日志 |
| `AutoCreationTaskMaterial` | 4,336 | 24 | 海阅素材路径 |
| `ImportVideoCnTvInfo` | 4,194 | 11 | 国剧导入 |
| `AIMixedClipSubTask` | 3,938 | 42 | AI 混剪子任务 |
| `AutoCreationSubTask` | 3,532 | 33 | 海阅子任务 |
| `DcMaterialInfo` | 3,471 | 51 | DC 素材信息 |
| `AIMixedClipMaterial` | 3,047 | 18 | AI 混剪素材 |
| `DynamicBackgroundTaskLog` | 2,010 | 7 | 动态背景日志 |
| `MaterialSyncTask` | 1,996 | 19 | 素材同步任务 |
| `UploadFileLog` | 1,630 | 15 | 文件上传日志 |

### 2.3 素材管理模块（26 表）— 最核心

```
MaterialInfo (237,570) ← 核心素材主表
  ├── MaterialUploadLog (226,029) ← 上传日志，AssetGuid 唯一标识
  ├── MaterialBurnJob (796) ← 烧录任务，JobId 关联大模型
  ├── MaterialConvertCoreTask (896) ← C1→C4 转码
  ├── MaterialSyncTask (1,996) ← 同步主任务
  │     └── MaterialSyncSubTask (1,102) ← 同步子任务
  ├── MaterialFission (裂变)
  │     ├── MaterialFissionDetail (602)
  │     ├── MaterialFissionAuditLog (530)
  │     └── MaterialFissionRequestLog (526)
  ├── MaterialFile (525) ← 素材文件
  ├── MaterialMd5Task ← MD5 校验
  ├── MaterialScore ← 素材评分
  ├── MaterialAutoSyncTask ← 自动同步
  └── MaterialElement / MaterialElementTag ← 元素标签
```

**关键关联**：
- `MaterialInfo.Id = MaterialBurnJob.MaterialId = MaterialUploadLog.MaterialId`
- `MaterialUploadLog.AssetGuid` 是素材报表唯一标识
- `MaterialUploadLog.UploadStatus = 4` 表示上传成功

### 2.4 AI 混剪模块（12 表）— 海剧自动化

```
AIMixedClipAssetGuid (746) ← 下发模型入参
  └── AIMixedClipTask (234) ← 混剪主任务
        └── AIMixedClipSubTask (3,938) ← 子任务
              ├── AIMixedClipRes (1,460) ← 生成资源
              ├── AIMixedClipTaskLog (1,075) ← 任务日志
              ├── AIMixedClipOcrTask (741) ← OCR 字幕
              └── AIMixedSubtitleRemovalTask (741) ← 字幕抹除
```

**状态码**：1=待处理, 2=处理中, 3=完成, 4=失败, 999=删除

### 2.5 海阅自动化模块（9 表）

```
AutoCreationTask (1,568) ← 主任务（存储传输文案）
  └── AutoCreationSubTask (3,532) ← 子任务拆分
        └── AutoCreationTaskMaterial (4,336) ← 素材路径
              └── AutoCreationTaskMaterialOpen (228) ← KOC 水印同步
```

### 2.6 国剧模块（7 表）

```
VideoCnBurnTask (69) ← 烧录主任务
  └── VideoCnBurnSubTask (271) ← 烧录子任务
        └── VideoCnBurnResource (93) ← 水印资源

VideoCnAdxUploadVideoTask (79) ← ADX 上传任务
  └── VideoCnAdxUploadVideo (69) ← ADX 上传视频

VideoCnCleanMaterialInfo (44) ← 纯净版视频
```

---

## 🤖 三、ai_center（30 表）— AI 任务调度中心

### 3.1 模块全景

| 模块 | 表数 | 总行数 | 核心职责 |
|------|------|--------|----------|
| **文件下载** | 4 | ~3436 万 | 海剧内容后台文件下载 |
| **任务管理** | 6 | ~304 万 | AI 任务、参数、结果、日志 |
| **消息/监控** | 3 | ~589 万 | MQM 监控、消息推送 |
| **口语裂变/剧集** | 3 | ~8.7 万 | 口语裂变日志、剧集信息 |
| **对象/章节** | 3 | ~21 万 | 书籍、章节、目录 |
| **DRP** | 4 | ~9,400 | DRP 文件上传 |
| **其他** | 6 | ~9.7 万 | 视频合并、错误码、字幕抹除 |

### 3.2 核心表 TOP 10

| 表名 | 行数 | 用途 |
|------|------|------|
| `file_download_task` | 34,309,884 | **最大表**，海剧视频文件下载 |
| `mqm_monitor_log_term_dictionary` | 5,735,751 | MQM 监控词库 |
| `TaskLog` | 2,348,002 | AI 任务执行日志 |
| `TaskArg` | 483,169 | AI 任务参数（键值对） |
| `ObjectCatChapter` | 206,075 | 书籍章节目录 |
| `SendChannelMessageLog` | 145,081 | 渠道消息推送日志 |
| `TaskInfo` | 126,076 | **AI 任务主表** |
| `oral_fission_log` | 87,316 | 口语裂变日志（OCR 数据） |
| `TaskResult` | 63,670 | AI 任务结果 |
| `fission_download_ref` | 62,023 | 裂变下载引用 |

### 3.3 任务调度核心链路

```
TaskInfo (126,076) ← AI 任务主表
  ├── TaskArg (483,169) ← 任务参数 (ArgName/ArgValue)
  ├── TaskLog (2,348,002) ← 执行日志
  ├── TaskResult (63,670) ← 任务结果 (Data 存 JSON)
  └── TaskRetryRecords ← 重试记录

oral_fission_log (87,316) ← 口语裂变
  └── source_series_file_task / source_series_info ← 剧集信息

file_download_task (34,309,884) ← 文件下载
  └── file_download_cache / file_download_summary
```

---

## 🌍 四、sharpengine_ads_global（617 表）— 全球广告库

### 4.1 模块全景

| 模块 | 表数 | 核心职责 |
|------|------|----------|
| **广告投放** | ~200+ | Facebook/TikTok/Google/Applovin/Snapchat 广告 |
| **资产管理** | ~50+ | 广告资产、素材、LTV、评分 |
| **创建编排** | ~80+ | 广告创建、模板、计划、文件夹 |
| **账户管理** | ~30+ | 账户、代理、预算、违规 |
| **数据洞察** | ~60+ | 日报、LTV、ROI、收入 |
| **视频/剧集** | ~20+ | VideoInfo、剧集、消费 |
| **BI/报表** | ~30+ | 书籍消费、ROI、排行榜 |
| **系统/配置** | ~100+ | 配置、日志、调度、同步 |

### 4.2 核心表 TOP 20（按行数）

| 表名 | 行数 | 用途 |
|------|------|------|
| `AdMobMediationReport` | 72,514,667 | AdMob 中介报告 |
| `AdMobMediationReportEst` | 69,141,315 | AdMob 预估报告 |
| `FbAdLtvItemInstallReferrerSnap` | 36,847,069 | FB 广告 LTV 快照 |
| `FbAdCountryDailyInsight` | 17,471,993 | FB 国家日报 |
| `FbAdLtvItemCountryInstallReferrer` | 15,736,563 | FB LTV 国家安装 |
| `FbAdLtvItemPayList` | 14,081,140 | FB LTV 付费列表 |
| `FbAdLtvItemInstallReferrerBackup` | 6,469,140 | FB LTV 备份 |
| `FbUpdateAccountAdIdInfo` | 6,356,780 | FB 账户广告 ID 更新 |
| `FbAccountInsight` | 5,537,397 | FB 账户洞察 |
| `FbAdAssetLog` | 2,918,329 | FB 广告资产日志 |
| `FbAdOperateCommonLog` | 2,546,474 | FB 操作日志 |
| `FbProject` | 2,439,984 | FB 项目 |
| `FbAdRoiInstallReferrer` | 2,386,045 | FB ROI 安装（737 列！） |
| `AppChannelCountryPutData` | 1,694,256 | 应用渠道国家投放 |
| `FbAdDailyInsight` | 1,574,544 | FB 广告日报 |
| `FbAdLtvItemInstallReferrer` | 1,472,841 | FB LTV 安装 |
| `BookConsumeLtvItemCountry` | 1,457,086 | 书籍消费 LTV |
| `BookRoiLtvItemCountry` | 1,440,867 | 书籍 ROI LTV |
| `FbAdLtvItemCountryPay` | 1,383,108 | FB LTV 国家付费 |
| `FbAdLtvItemPay` | 1,316,286 | FB LTV 付费 |

### 4.3 关键业务表

#### 剧集/视频
| 表名 | 用途 |
|------|------|
| `VideoInfo` | **海剧剧集主表**（VideoId, Language, AllEpis, CoverUrl） |
| `VideoLabel` | 剧集标签 |
| `VideoLanguage` | 剧集语种 |

#### 广告账户
| 表名 | 用途 |
|------|------|
| `AdsAccount` | 广告账户 |
| `AccountChangeLog` | 账户变更日志 |
| `AccountViolationReport` | 账户违规报告 |
| `AdAccountAgent` | 广告代理 |

#### 广告创建
| 表名 | 用途 |
|------|------|
| `AdsCreationTemplate` | 创建模板 |
| `AdsCreationPlan` | 创建计划 |
| `AdsCreationFolder` | 创建文件夹（按渠道分） |
| `AdsCreationFiles` | 创建文件（按渠道分） |

---

## 🔗 五、跨库关联关系

### 5.1 海剧自动化全链路

```
sharpengine_ads_global.VideoInfo (剧集信息)
  └─ VideoId ──────────────────────┐
                                    ↓
sharpengine_ads_asset_prod.TaskInfo.TgtId
  └─ TaskInfo.Id ──────────────────┐
                                    ↓
sharpengine_ads_asset_prod.AIMixedClipTask
  └─ AIMixedClipSubTask.TaskId
       ├─ AIMixedClipRes (生成资源)
       └─ JobId ───────────────────┐
                                    ↓
ai_center.TaskInfo (AI 模型任务)
  └─ TaskArg / TaskResult
```

### 5.2 海阅自动化全链路

```
sharpengine_bi.if_books (书籍标签)
  └─ bookid ───────────────────────┐
                                    ↓
sharpengine_ads_asset_prod.AutoCreationTask
  └─ AutoCreationSubTask
       └─ AutoCreationTaskMaterial
            ├─ MaterialInfo
            ├─ MaterialBurnJob
            └─ AutoCreationTaskMaterialOpen (KOC 水印)
                 └─ AsyncJobInfo (大模型交互)
```

### 5.3 素材报表链路

```
MaterialInfo.Id = MaterialBurnJob.MaterialId = MaterialUploadLog.MaterialId
MaterialUploadLog.AssetGuid ← 素材报表唯一标识
MaterialUploadLog.UploadStatus = 4 ← 上传成功
```

### 5.4 同步链路

```
MaterialUploadLog.SourceUploadLogId
  └─ MaterialSyncTask.SourceUploadLogId
       └─ MaterialSyncSubTask.TaskId
```

---

## 📋 六、常用查询速查

### 6.1 查素材报表数据
```sql
SELECT * FROM MaterialUploadLog WHERE AssetGuid = 'xxx';
SELECT * FROM MaterialInfo WHERE Id = 3746994889975180720;
```

### 6.2 查 AI 混剪任务
```sql
SELECT * FROM AIMixedClipTask WHERE Id = 10420036;
SELECT * FROM AIMixedClipSubTask WHERE TaskId = 10420036;
```

### 6.3 查海阅自动化任务
```sql
SELECT * FROM AutoCreationTask WHERE Id = xxx;
SELECT * FROM AutoCreationTaskMaterial WHERE SubTaskId = xxx;
```

### 6.4 查剧集信息
```sql
SELECT * FROM sharpengine_ads_global.VideoInfo WHERE VideoId = '14583322';
```

### 6.5 查 AI 中心任务
```sql
SELECT * FROM ai_center.TaskInfo WHERE Id = xxx;
SELECT * FROM ai_center.TaskArg WHERE TaskId = xxx;
```

### 6.6 查同步数据
```sql
SELECT * FROM MaterialSyncTask WHERE SourceUploadLogId = xxx;
SELECT * FROM MaterialSyncSubTask WHERE TaskId = xxx;
```

---

## ⚠️ 七、注意事项

1. **大表警告**：`sharpengine_ads_global` 有多张千万级甚至亿级表，查询务必带索引条件
2. **TiDB 特性**：所有库都在 TiDB 集群上，支持分布式事务，但大表 COUNT(*) 可能较慢
3. **配置安全**：`database.ini` 已加入 `.gitignore`，不要提交到 Git
4. **测试环境**：文档中提到的 `ai_center_test`、`short_video_admin_test9` 等测试库未在 `database.ini` 中配置
5. **sharpengine_bi**：海阅书籍标签库 `if_books` 未在 `database.ini` 中配置，需要时需手动添加

---

> **本文档由 AI 自动扫描生成，扫描时间 2026-04-28，涵盖 3 个数据库共 780 张表。**
