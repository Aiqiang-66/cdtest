# 文案格式 正向/逆向用例 结果报告（日语 · 韩语）

> **用例集**：`测试用例/文案格式_正常与异常_0924.md`（**19 条**：正常 6 / 异常 13）  
> **环境**：`https://ai-main-none-dev.changdu.ltd`｜`model=higgs`｜日语 `lang=9/Japanese_female`、韩语 `lang=14/Korean_female`  
> **语料**：M0 合规（保留段落）~5000 字符单元（`..._r3/material`）  
> **日期**：2026-09-24　**执行器**：`run_text_format_cases_0924.py --workers 4 --out-suffix _{ja|ko}`

## 一、结果总览

| 语种 | PASS | FAIL | ERROR | INFO | 结论 |
|---|---:|---:|---:|---:|---|
| 日语 | 6 | 4 | 0 | 9 | ❌ 存在缺陷 |
| 韩语 | 6 | 3 | 0 | 10 | ❌ 存在缺陷 |

> INFO = 异常场景用例，按设计只记录实际行为、不判失败；FAIL 全部集中在两条硬判据上（见第二节）。

## 二、FAIL 明细（两语种共性缺陷）

| 用例 | 日语 | 韩语 | 判读 |
|---|---|---|---|
| TC-FMT-A04a 空文本 | ❌ FAIL：期望报错，但任务成功并产出了音频 | ❌ FAIL：期望报错，但任务成功并产出了音频 | **参数校验缺失**：应拒绝却成功产出音频 |
| TC-FMT-A04b 纯空白文本 | ❌ FAIL：期望报错，但任务成功并产出了音频 | ❌ FAIL：期望报错，但任务成功并产出了音频 | **参数校验缺失**：应拒绝却成功产出音频 |
| TC-FMT-A10 非法参数 lang=999 | ❌ FAIL：期望报错，但任务成功并产出了音频 | ❌ FAIL：期望报错，但任务成功并产出了音频 | **参数校验缺失**：应拒绝却成功产出音频 |
| TC-FMT-N01 标准单元（保留段落 ~5000 字） | ❌ FAIL：高语速段 1 个（最大 25.9 字/秒） | ✅ PASS：全部判据通过 |  |

### 缺陷 1（P1）：参数校验缺失 —— 空文本 / 纯空白 / 非法 `lang` 均返回成功并产出音频

| 用例 | 输入 | 期望 | 实际（两语种一致） |
|---|---|---|---|
| TC-FMT-A04a | `read_content = ""` | 报错，不产出 | **任务成功（taskStatus=2）并生成音频/metadata** |
| TC-FMT-A04b | `read_content = "   "` | 报错，不产出 | **任务成功并生成音频** |
| TC-FMT-A10 | `lang = 999`（非法语种） | 报错或明确回退 | **任务成功并生成音频**（未回退、未报错） |

> 影响：调用方传错参数时会拿到「成功」的空/垃圾音频，问题被推迟到播放环节才暴露；`lang` 不校验也无法保证音色与语种一致。建议服务端在**建任务前**做参数白名单校验（`read_content` 去空白后长度 ≥1、`lang`/`voice`/`model` 必须在支持列表内），非法即返回明确错误码。

## 三、异常场景实际行为（INFO，供开发参考）

| 用例 | 输入规模 | 日语 | 韩语 |
|---|---|---|---|
| TC-FMT-A02 无句末标点的长文（10 段 ~2000 字） | 3309 字 | paraIndex=10｜段数=50｜最长段=65字｜最大语速=9.8｜81.64s=False｜缺失字=0 | paraIndex=10｜段数=50｜最长段=65字｜最大语速=6.5｜81.64s=False｜缺失字=0 |
| TC-FMT-A03 单段超长且无标点（~3000 字） | 2880 字 | paraIndex=1｜段数=45｜最长段=63字｜最大语速=6.4｜81.64s=False｜缺失字=0 | paraIndex=1｜段数=45｜最长段=63字｜最大语速=6.1｜81.64s=False｜缺失字=0 |
| TC-FMT-A05 特殊字符（HTML/emoji/控制符/零宽） | 57 字 | paraIndex=4｜段数=4｜最长段=20字｜最大语速=12.5｜81.64s=False｜缺失字=0 | paraIndex=4｜段数=4｜最长段=20字｜最大语速=9.7｜81.64s=False｜缺失字=0 |
| TC-FMT-A06 大量重复句（同一句 100 次，10 段） | 1299 字 | paraIndex=10｜段数=20｜最长段=72字｜最大语速=11.6｜81.64s=False｜缺失字=0 | paraIndex=10｜段数=20｜最长段=77字｜最大语速=8.3｜81.64s=False｜缺失字=0 |
| TC-FMT-A07 换行爆炸（200 个单字段落） | 399 字 | paraIndex=200｜段数=200｜最长段=1字｜最大语速=3.2｜81.64s=False｜缺失字=0 | paraIndex=200｜段数=200｜最长段=1字｜最大语速=3.5｜81.64s=False｜缺失字=0 |
| TC-FMT-A08 混合语种（中/英/日，4 段） | 128 字 | paraIndex=4｜段数=4｜最长段=60字｜最大语速=14.8｜81.64s=False｜缺失字=0 | paraIndex=4｜段数=4｜最长段=60字｜最大语速=16.5｜81.64s=False｜缺失字=0 |
| TC-FMT-A09 超长文本（3 单元拼接 ~15000 字） | 15781 字 | paraIndex=204｜段数=386｜最长段=80字｜最大语速=27.5｜81.64s=False｜缺失字=0 | paraIndex=205｜段数=390｜最长段=80字｜最大语速=10.3｜81.64s=False｜缺失字=0 |
| TC-FMT-A11 纯标点（5 段） | 74 字 | paraIndex=5｜段数=5｜最长段=14字｜最大语速=43.6｜81.64s=False｜缺失字=0 | paraIndex=5｜段数=5｜最长段=14字｜最大语速=43.8｜81.64s=True｜缺失字=0 |
| TC-FMT-A12 单句超长（1 句 ~1000 字） | 977 字 | paraIndex=1｜段数=13｜最长段=80字｜最大语速=10.3｜81.64s=False｜缺失字=0 | paraIndex=1｜段数=13｜最长段=80字｜最大语速=7.8｜81.64s=False｜缺失字=0 |

## 四、关键观察

1. **M0 前置有效**：保留段落（正/逆向的 N 组）在**两语种**均被正确识别（`distinct paraIndex` 随段落数增长，段长中位 30~40 字、无 >200 字超长段）。
2. **格式违规（单段落）复现存在语种差异**：日语 A01 复现退化（`paraIndex=1`，高语速段 2 个，最大 75.1 字/秒）；韩语 A01 **未复现**（`paraIndex=1` 但最长段仅 80 字、最大语速 9.2）→ 说明**单段落输入仍是风险写法**（日语可退化、韩语本轮未触发），M0 规则必须保留。
3. **日语 N01 出现 1 个高语速段（25.9 字/秒）**：正常用例里也偶发段级时间轴异常，建议纳入 Phase 9 段级观察项持续跟踪。
4. **超长文本（A09，15.7K 字）两语种均成功**：说明接口可处理远超 5000 字的文本，但时长/段数分布需在报告中单独列示（见 results.json）。
5. **韩语纯标点输入触发 81.64s 固定时长段（静默占位特征）**：A11 输入仅 74 个标点字符，返回中出现 `81.64s` 段（与 0924 批次日语 0035 / 韩语 0044 的静默占位时长完全一致）。建议服务端对「无实义词文本」直接拒绝或跳过，而不是插入占位段；该现象应纳入 Phase 9 回归项持续跟踪。
6. **段级时间轴仍有零星异常**：A09（15.7K 字）日语最大语速 27.5 字/秒、A11（纯标点）两语种 43+ 字/秒、A08（混合语种）韩语 16.5 字/秒 —— 均属「段时长与文本量不匹配」，建议作为 Phase 9 段级观察项记录，不单独判缺陷（内容口径 M1 均为 0 缺失）。

## 五、附件索引

| 内容 | 路径（相对仓库根） |
|---|---|
| 日语 用例结果（逐用例指标+判定） | `docs\听书测试物料\mp3\文案格式用例_0924_ja\results.json` |
| 日语 实际下发文本 / 返回指标 | `docs\听书测试物料\mp3\文案格式用例_0924_ja\src\`、`docs\听书测试物料\mp3\文案格式用例_0924_ja\meta\` |
| 日语 输入规模清单 | `docs\听书测试物料\mp3\文案格式用例_0924_ja\case_inputs.json` |
| 韩语 用例结果（逐用例指标+判定） | `docs\听书测试物料\mp3\文案格式用例_0924_ko\results.json` |
| 韩语 实际下发文本 / 返回指标 | `docs\听书测试物料\mp3\文案格式用例_0924_ko\src\`、`docs\听书测试物料\mp3\文案格式用例_0924_ko\meta\` |
| 韩语 输入规模清单 | `docs\听书测试物料\mp3\文案格式用例_0924_ko\case_inputs.json` |
| 用例定义 | `测试用例\文案格式_正常与异常_0924.md` |
